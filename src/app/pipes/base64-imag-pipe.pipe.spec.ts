import { Base64ImagPipePipe } from './base64-imag-pipe.pipe';

describe('Base64ImagPipePipe', () => {
  it('create an instance', () => {
    const pipe = new Base64ImagPipePipe();
    expect(pipe).toBeTruthy();
  });
});
