import { UsedToInrPipe } from './used-to-inr.pipe';

describe('UsedToInrPipe', () => {
  it('create an instance', () => {
    const pipe = new UsedToInrPipe();
    expect(pipe).toBeTruthy();
  });
});
